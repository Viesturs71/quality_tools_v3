"""
Debug Tools models initialization.
This package doesn't contain actual models but is included for structural consistency.
"""
# No models are defined in this package as debug tools typically don't require database storage
